# Bluetooth widget

The Bluetooth widget is a portable bar or desktop widget backed by the shared
`BluetoothService`. Its compact form opens the Bluetooth drawer, where BlueZ
devices can be scanned, connected, disconnected, and powered on or off.

It uses `bluetoothctl` from `bluez-utils`; it does not require Blueman.
