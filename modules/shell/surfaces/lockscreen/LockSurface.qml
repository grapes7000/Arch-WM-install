import QtQuick
import Quickshell

// Disabled until authentication, secure layer behavior, capability filtering
// and crash recovery are implemented and tested independently.
Scope {
    property bool enabled: false
}
