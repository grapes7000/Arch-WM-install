import QtQuick
import Quickshell
import ".."

// Scaffold only. The production implementation must create one desktop-layer
// host per screen and populate it entirely from desktop layout JSON.
Scope {
    property bool enabled: false
}
